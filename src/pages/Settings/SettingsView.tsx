import { SettingsTab } from "components/layouts/dashboard/settings";

const SettingsView = () => {
  // eslint-disable-next-line prettier/prettier
  return (
    <div>
      <SettingsTab />
    </div>
  );
};

export default SettingsView;
